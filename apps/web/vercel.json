{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "node tools/generate-llms.js || true && vite build",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
